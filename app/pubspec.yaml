name: scasi_standalone
description: Flutter client for Scasi — real Gmail inbox, Supabase-authenticated, backed by the FastAPI server in /backend for AI features and chat history.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Auth: native Google Sign-In (Gmail scope) + Supabase session on top of it
  google_sign_in: ^6.2.2
  supabase_flutter: ^2.6.0

  # Networking (backend REST + SSE chat)
  http: ^1.2.2

  # Formatting
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
